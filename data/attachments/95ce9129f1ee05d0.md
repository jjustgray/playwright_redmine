# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: searchFunction.spec.ts >> Redmine tests >> TC-02: Search function by words in headers
- Location: tests/searchFunction.spec.ts:6:7

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator:  locator('dl#search-results dt a').first()
Expected: "Error"
Received: "Defect #44361 (New): Preview returns a 404 error on the cross-project news form"
Timeout:  5000ms

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('dl#search-results dt a').first()
    13 × locator resolved to <a href="/issues/44361">…</a>
       - unexpected value "Defect #44361 (New): Preview returns a 404 error on the cross-project news form"

```

```yaml
- 'link "Defect #44361 (New): Preview returns a 404 error on the cross-project news form"':
  - /url: /issues/44361
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import MainPage from '../pages/main.page';
  3  | import SearchPage from '../pages/search.page';
  4  | 
  5  | test.describe('Redmine tests', () => {
  6  |   test('TC-02: Search function by words in headers', async ({ page }) => {
  7  |     await page.goto('https://www.redmine.org/');
  8  | 
  9  |     const mainPage = new MainPage(page);
  10 |     await expect(mainPage.searchButton).toBeVisible();
  11 |     await mainPage.findAndClickSearchButton();
  12 | 
  13 |     const searchPage = new SearchPage(page);
  14 |     await expect(searchPage.searchForm).toBeVisible();
  15 |     await expect(searchPage.titlesOnlyOption).toBeVisible();
  16 |     await searchPage.findAndClickTitlesOnlyOption();
  17 |     await searchPage.enterTextForSearch('Error');
  18 |     await expect(searchPage.submitButton).toBeVisible();
  19 |     await searchPage.submitSearch();
  20 |     await expect(searchPage.resultsTable).toBeVisible();
  21 |     
  22 |     const recordCount = await searchPage.recordTitles.count();
  23 |     expect(recordCount).toBeGreaterThan(3);
  24 |     for (let i = 0; i < 3; i++) {
> 25 |       await expect(searchPage.recordTitles.nth(i)).toHaveText('Error');
     |                                                    ^ Error: expect(locator).toHaveText(expected) failed
  26 |     }
  27 |   });
  28 | });
  29 | 
```